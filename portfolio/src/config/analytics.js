// View counters use CounterAPI (https://counterapi.dev) — a free service since a static
// GitHub Pages site has no backend of its own to store counts in.
//
// To turn the counters on:
//   1. Sign up free at https://counterapi.dev/signup (no credit card)
//   2. Create a workspace (e.g. "andrew-portfolio") from the dashboard
//   3. Generate an API key from the dashboard
//   4. Paste both values below
//
// Until both are filled in, the counters simply stay invisible — nothing else on the
// site breaks. Note: because this is a static site with no server, the API key is
// visible to anyone who views the page source. CounterAPI's free tier is rate-limited
// (600 requests/minute) which keeps casual abuse in check, but don't reuse a key that
// guards anything more sensitive than a view count.

export const ANALYTICS_WORKSPACE = ''
export const ANALYTICS_API_KEY = ''
